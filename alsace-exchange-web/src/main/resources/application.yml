#============
# logging配置
#============
logging:
  level:
    root: info
  file:
    max-history: 7
    max-size: 10MB
    name: ${spring.application.name}

server:
  port: 8080

spring:
  application:
    name: alsace-exchange-web
  profiles:
    active: dev